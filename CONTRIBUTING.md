# コントリビューションガイド

本プロジェクトへのコントリビューションに感謝します。円滑な開発を進めるために、以下のガイドラインに従ってください。

## 1. 開発環境のセットアップ

1. リポジトリをクローンします。
   ```bash
   git clone <repository_url>
   cd vibe-spec-ssot
   ```
2. （必要に応じて）環境変数を設定します。
   ```bash
   cp .env.example .env
   ```
3. このリポジトリはドキュメントベースのSSOT管理が主目的であるため、特別なビルドツールやパッケージマネージャーのインストールは不要です。Markdownが編集可能な好みのエディタ（VS Code推奨）をご利用ください。

## 2. ブランチの命名規則

作業を行う際は、用途に応じて以下のプレフィックスを付けてブランチを作成してください。

* `feature/` : 新しい機能の追加（例: `feature/add-new-spec`）
* `fix/` : バグ修正や誤記の訂正（例: `fix/typo-in-docs`）
* `docs/` : ドキュメントのみの変更・追加（例: `docs/update-index`）

例:
```bash
git checkout -b feature/issue-123
```

## 3. コミットメッセージのフォーマット

本リポジトリでは [Conventional Commits](https://www.conventionalcommits.org/ja/v1.0.0/) の規約を採用しています。コミットメッセージの先頭には、変更内容を示す型（type）を記述してください。

**形式:**
```
<type>: <description>
```

**主な type:**
* `feat`: 新機能の追加
* `fix`: バグの修正
* `docs`: ドキュメントのみの変更
* `style`: コードの意味に影響を与えない変更（空白、フォーマットなど）
* `refactor`: バグ修正や機能追加ではないコードの変更
* `test`: テストの追加・修正
* `chore`: ビルドプロセスや補助ツールの変更

**例:**
* `docs: READMEの更新ルールを追記`
* `feat: 新しい用語をglossaryに追加`

## 4. PR（Pull Request）のレビュープロセス

1. 作業が完了したら、リモートリポジトリに変更をプッシュします。
2. GitHub上でPull Request（PR）を作成します。
3. PRの説明欄に、変更の目的と概要を簡潔に記載してください。関連するIssueがあればリンクを含めます。
4. 本リポジトリのルール（`CLAUDE.md` 参照）に従い、特に以下を確認してください。
   * **docs/** への変更か、**sources/** を改変していないか。
   * 方針変更を伴う場合は **decisions/** にADRを先に追加したか。
5. レビュアーからのフィードバックがあれば対応し、承認（Approve）を得た後にメインブランチにマージします。
